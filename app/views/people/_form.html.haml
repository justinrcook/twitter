= simple_form_for(@person) do |f|
  = f.error_notification

  .form-inputs
    = f.input :username, :label => "Twitter Username/Handle"

  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to 'Cancel', '#', :class => 'btn'
